import React from 'react';

const DeviceDetails = (props) => {
    return (
        <div>
            <p>it is price {props.price}</p>
        </div>
    );
};

export default DeviceDetails;

/* 
props unidirectional flow
props er value change kora jay na
*/